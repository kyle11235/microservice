# devops

deploy

    place start.sh into source code
    install git/docker
    git clone xxx
    cd xxx && build app
    chmod +x *.sh
    ./start.sh

redeploy

    git checkout -- *.sh
    git pull